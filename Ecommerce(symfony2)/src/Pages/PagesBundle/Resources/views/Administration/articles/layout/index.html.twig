{% extends '::layout/layoutSuperAdmin.html.twig' %}

{% block body -%}
    <div class="new_title center">
              <h2> Listes des articles </h2>
            </div>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Description</th>
                <th>Contenu</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.titre }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.contenu }}</td>
                
                 <td>
                    <a href="{{ path('adminArticles_show', { 'id': entity.id }) }}"><i class="icon-book"></i></a>
                     <a href="{{ path('adminArticles_edit', { 'id': entity.id }) }}"><i class="icon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="bouton_button_add">
            <a href="{{ path('adminArticles_new') }}">
                Créer une nouveau article
            </a>
    </div>
        
    </ul>

    {% endblock %}


