{% extends '::layout/layoutSuperAdmin.html.twig' %}

{% block body -%}
    <div class="new_title center">
        <h2> Liste des Produits Premium </h2>
    </div>
    
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>&nbsp;</th>
                <!--<th>Id</th>--!>
                <th>Nom</th>
                <th>Marque</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Prix remise</th>
                <th>Disponible</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td class="image"><a class="product-image" title="{{ entity.nom }}" href="{{ path('presentationPremium', { 'idP' : entity.id }) }}"><img width="75" alt="{{ entity.nom }}" src="{{ asset(entity.image.AssetPath) }}"></a></td>
                <!--<td><a href="{{ path('adminProduitsPremium_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>--!>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.marque }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.prix }}</td>
                <td>{{ entity.prixremise }}</td>
                <td>{{ entity.disponible }}</td>
                <td>
                    <a href="{{ path('adminProduitsPremium_show', { 'id': entity.id }) }}"><i class="icon-book"></i></a>
                    <a href="{{ path('adminProduitsPremium_edit', { 'id': entity.id }) }}"><i class="icon-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="bouton_button_add">
        <a href="{{ path('adminProduitsPremium_new') }}">
                Créer un nouveau produit Premium
        </a>
    </div>
    {% endblock %}
