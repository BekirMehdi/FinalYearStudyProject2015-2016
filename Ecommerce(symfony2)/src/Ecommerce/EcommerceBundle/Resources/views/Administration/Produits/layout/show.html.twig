{% extends '::layout/layoutAdmin.html.twig' %}
{% block body -%}
<div class="new_title center">
    <h2>Produits</h2>
</div>
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>&nbsp;</th>
                <!--<th>Id</th>--!>
                <th>Nom</th>
                <th>Marque</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Prix remise</th>
                <th>Disponible</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td class="image"><a class="product-image" title="{{ entity.nom }}" href="{{ path('presentationBoutique', { 'idB' : entity.id }) }}"><img width="75" alt="{{ entity.nom }}" src="{{ asset(entity.image.AssetPath) }}"></a></td>
                <!--<td><{{ entity.id }}</td>--!>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.marque }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.prix }}</td>
                <td>{{ entity.prixremise }}</td>
                <td>{{ entity.disponible }}</td>
             </tr>                                 
        </tbody>
    </table>

  <br>
    <div class="bouton_button_return">
        <a href="{{ path('adminProduits') }}">
            Retour à la liste
        </a>
    </div>    
 
<br>
    <div class="bouton_button_add">
        <a href="{{ path('adminProduits_edit', { 'id': entity.id }) }}">
            modifier
        </a>
    </div>
    <br>
    <div>{{ form(delete_form) }}</div>

{% endblock %}
